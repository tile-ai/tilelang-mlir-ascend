# 质量自检清单

生成 `DESIGN.md` 后，逐项检查：

| # | 检查项 | 是否必须通过 |
|---|--------|-------------|
| 1 | **编程模式有明确结论和理由**：不是笼统的「视情况而定」 | ✅ 必须 |
| 2 | **API 映射具体到函数名和参数**：不是「使用相关 API」 | ✅ 必须 |
| 3 | **内存搬运路径完整**：从 GM 到计算再到 GM 的每一步都有说明 | ✅ 必须 |
| 4 | **Tiling 策略有约束分析**：解释了为什么选择该 Block/Tile 大小 | ⭕ 推荐 |
| 5 | **同步策略与编程模式匹配**：Developer 用自动同步、Expert 标明手动同步点 | ⭕ 推荐 |
| 6 | **验证方案含 Golden 函数实现和输出精度对比语句**：Golden 参考实现完整 | ⭕ 推荐 |
| 7 | **无占位符或模糊描述**：无 `{placeholder}`、TODO、「待补充」（已确认的除外） | ✅ 必须 |
| 8 | **技术约束已确认**：三维 Kernel等问题已处理 | ✅ 必须 |
| 9 | **含 GEMM 场景**：Tiling 策略满足 NPU 分形限制（block_M ≥ 16, block_N ≥ 16） | ✅ 必须 |
| 10 | **含 GEMM 场景 L0C 容量约束验证**：`block_M × block_N × sizeof(accum_dtype) ≤ L0C_capacity (128KB)` | ⭕ 推荐 |
| 11 | **含 GEMM 场景非整除处理策略明确**：主机侧 padding+crop 或 Kernel 内动态 block，说明溢出 / 下溢处理 | ✅ 必须 |
| 12 | **本项目同类实现已列出**：有具体的 examples/ 文件路径参考 | ✅ 必须 |
| 13 | **参考实现差异已说明**：如有外部参考，列出 API/结构差异 | ⭕ 推荐 |
| 14 | **参考实现分析完整**：如有外部参考，记录内存层级 API、同步策略、pass_configs 等技术决策 | ⭕ 推荐 |
| 15 | **参考实现标注原算子路径**：如有外部参考，标注文件路径，用于获取 golden 实现 | ⭕ 推荐 |
| 16 | **参考实现标注输出形状**：如有外部参考，说明输出形状是否需要 transpose | ⭕ 推荐 |
| 17 | **函数无全局变量依赖**：维度参数从 tensor shape 或函数参数获取，支持多场景顺序测试 | ⭕ 推荐 |
| 18 | **分核策略三要素齐全**：逻辑核数计算（`ceil(M/block_M) × ceil(N/block_N)`）、物理核数及来源（假设显式标注）、规模判定与分核方案（逻辑核数 ≤ 物理核数给依据 / 中等规模对齐物理核整数倍 / 极大规模核内 `T.serial` 串行且边界静态），依据 docs/开发指南.md §3.3 | ✅ 必须 |
| 19 | **数学等价优化分析完整**：§1.6.1 逐项有「原式 → 优化后公式 → 等价性论证 → 收益估算」四要素；无优化空间时写明结论与依据；优化后公式与 §3.1 一致 | ✅ 必须 |
| 20 | **向量化替代分析完整**：§1.6.2 覆盖实现方案中全部循环 / 标量计算点，替代方案 API 有 examples/docs 佐证；不可替代项逐项给出充分理由（block 索引 / host 元数据 / 顺序依赖 / 动态边界 / API 缺失佐证），与 §6 循环结构一致 | ✅ 必须 |
| 21 | **向量化轴与布局决策完整**：§1.6.3 含候选矩阵（布局 × 向量轴，含重排布局变体）与逐候选量化评分（整除性/尾 lane 浪费/累加链形态/repack 代价/UB 影响）；弃选方案有量化理由（禁止"未考虑"）；单轴算子有豁免说明；决策与 §3.3/§4/§6 一致（累加循环内层向量维 = 选定轴，buffer 形状 = 选定布局）；迁移任务对照了源码的隐式轴选择 | ✅ 必须 |

## 迁移专属检查项（迁移任务必须全部通过）

| # | 检查项 | 是否必须通过 |
|---|--------|-------------|
| M1 | **§0.1/0.2 语义与 I/O 和源码一致**：数学语义、规约语义（累加顺序）、dtype 语义、边界语义、输出 shape 均从源码解读得出，无臆测 | ✅ 必须 |
| M2 | **§0.3 算法解读完整**：源码每个计算语句归入某步骤（无遗漏、无臆造）；数据流图覆盖全部 buffer；host 侧逻辑已列出 | ✅ 必须 |
| M3 | **§0.4 优化手段清单完整**：源码中的显式优化（SMEM tiling / mma / warp shuffle / 异步流水 / swizzle / persistent / online 算法等）至少全部识别并列出，逐项标注硬件依赖 | ✅ 必须 |
| M4 | **§0.5 耦合性判定逐项有依据**：每个条目有四态处置（保留/等价替换/重新设计/舍弃）+ 依据（映射表条目 / examples / docs）；舍弃项写明理由 | ✅ 必须 |
| M5 | **§0.6 重设计项三要素齐全**：每个重设计项有「源方案 → NPU 新算法 → 语义保持论证」；无重设计项时写明结论与依据 | ✅ 必须 |
| M6 | **§1–§7 与迁移决策一致**：概述/API 映射/内存规划/Tiling/循环/同步描述的是 §0.5/§0.6 决策后的 NPU 算法，未照抄已被重设计/舍弃的源方案 | ✅ 必须 |
| M7 | **§8.1 golden 实现源算子语义**：以 §0.1 为依据（优先移植源仓参考实现），未复刻 NPU 算法 | ✅ 必须 |
| M8 | **迁移三问均有结论**：语义（What）/ 算法（How）/ 优化手段（Why fast）三问各有明确小结，无「待补充」 | ✅ 必须 |

**通过条件**：非迁移任务——必须项（1, 2, 3, 7, 8, 9, 12, 18, 19, 20）全部通过（9/11 仅含 GEMM 场景时适用），推荐项至少通过 4/9；迁移任务——在前述条件基础上，**M1–M8 全部通过**。
